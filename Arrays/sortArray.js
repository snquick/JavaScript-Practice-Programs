// Prompt: Write a JavaScript program to sort the items of an array

// function sortItems(arr) 
// array.sort()

arr = ['a', 'd', 'c', 'q', 'm'];

function sortItems(arr) {
    return arr.sort();
}

console.log(sortItems(arr));

